const state={page:'home',user:null,data:null,lang:localStorage.ec_lang||'en',theme:localStorage.ec_theme||'light',scale:Number(localStorage.ec_scale||0),voice:[],modal:null};
const T={en:{home:'Home',health:'My Health',meds:'Medicines',appts:'Appointments',family:'Family',docs:'Documents',emergency:'Emergency',settings:'Settings',admin:'Admin',login:'Sign in',email:'Email',password:'Password',welcome:'Welcome back',tag:'One safe place for health, medicines, and family care',today:'Today',overview:"Here’s what’s happening today.",blood:'Blood group',allergies:'Allergies',conditions:'Medical conditions',next:'Next appointment',taken:'Taken',pending:'Pending',missed:'Missed',mark:'Mark taken',skip:'Skip',remind:'Remind',call:'Call',message:'Message',edit:'Edit',save:'Save changes',cancel:'Cancel',delete:'Delete',upload:'Upload document',add:'Add appointment',logout:'Sign out',theme:'Theme',language:'Language',text:'Text size',voice:'Voice assistant',restricted:'This area is restricted for your role.',disclaimer:'This platform organizes health information and is not a substitute for professional medical advice or emergency services.',confirm:'Are you sure?',yes:'Continue',no:'Go back',demo:'Demo accounts',passwordHint:'Password for all demo accounts: Demo!Password123',adminNote:'Admins can manage account and audit information, but do not receive casual medical access.'},hi:{home:'होम',health:'मेरा स्वास्थ्य',meds:'दवाइयाँ',appts:'अपॉइंटमेंट',family:'परिवार',docs:'दस्तावेज़',emergency:'आपातकाल',settings:'सेटिंग्स',admin:'एडमिन',login:'साइन इन',email:'ईमेल',password:'पासवर्ड',welcome:'वापसी पर स्वागत है',tag:'स्वास्थ्य, दवाइयों और परिवार की देखभाल के लिए सुरक्षित जगह',today:'आज',overview:'आज की जानकारी यहाँ है।',blood:'ब्लड ग्रुप',allergies:'एलर्जी',conditions:'स्वास्थ्य स्थितियाँ',next:'अगला अपॉइंटमेंट',taken:'ली गई',pending:'बाकी',missed:'छूटी',mark:'ली गई चिन्हित करें',skip:'स्किप',remind:'रिमाइंड',call:'कॉल',message:'मैसेज',edit:'एडिट',save:'सेव',cancel:'रद्द',delete:'डिलीट',upload:'दस्तावेज़ अपलोड',add:'अपॉइंटमेंट जोड़ें',logout:'साइन आउट',theme:'थीम',language:'भाषा',text:'टेक्स्ट आकार',voice:'वॉइस असिस्टेंट',restricted:'आपकी भूमिका के लिए यह क्षेत्र सीमित है।',disclaimer:'यह प्लेटफ़ॉर्म स्वास्थ्य जानकारी व्यवस्थित करने के लिए है; यह डॉक्टर की सलाह या आपातकालीन सेवा का विकल्प नहीं है।',confirm:'क्या आप सुनिश्चित हैं?',yes:'जारी रखें',no:'वापस',demo:'डेमो अकाउंट',passwordHint:'सभी डेमो अकाउंट का पासवर्ड: Demo!Password123',adminNote:'एडमिन अकाउंट और ऑडिट जानकारी देख सकता है, सामान्य मेडिकल डेटा नहीं।'},mr:{home:'मुख्यपृष्ठ',health:'माझे आरोग्य',meds:'औषधे',appts:'अपॉइंटमेंट',family:'कुटुंब',docs:'कागदपत्रे',emergency:'आपत्कालीन',settings:'सेटिंग्ज',admin:'अॅडमिन',login:'साइन इन',email:'ईमेल',password:'पासवर्ड',welcome:'पुन्हा स्वागत आहे',tag:'आरोग्य, औषधे आणि कुटुंबाच्या काळजीसाठी सुरक्षित जागा',today:'आज',overview:'आजची माहिती येथे आहे.',blood:'ब्लड ग्रुप',allergies:'अॅलर्जी',conditions:'आरोग्य स्थिती',next:'पुढील अपॉइंटमेंट',taken:'घेतले',pending:'प्रलंबित',missed:'चुकले',mark:'घेतले असे चिन्हांकित',skip:'स्किप',remind:'रिमाइंड',call:'कॉल',message:'मेसेज',edit:'एडिट',save:'सेव्ह',cancel:'रद्द',delete:'डिलीट',upload:'कागदपत्र अपलोड',add:'अपॉइंटमेंट जोडा',logout:'साइन आउट',theme:'थीम',language:'भाषा',text:'टेक्स्ट आकार',voice:'व्हॉइस असिस्टंट',restricted:'तुमच्या भूमिकेसाठी हे क्षेत्र मर्यादित आहे.',disclaimer:'हे प्लॅटफॉर्म आरोग्य माहिती व्यवस्थित करण्यासाठी आहे; डॉक्टरांचा सल्ला किंवा आपत्कालीन सेवेचा पर्याय नाही.',confirm:'तुम्हाला खात्री आहे?',yes:'पुढे',no:'मागे',demo:'डेमो अकाउंट',passwordHint:'सर्व डेमो अकाउंटचा पासवर्ड: Demo!Password123',adminNote:'अॅडमिन खाते आणि ऑडिट माहिती व्यवस्थापित करू शकतो; सामान्य वैद्यकीय माहिती नाही.'}};
const t=()=>T[state.lang];
const esc=s=>String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
const api=async(url,opt={})=>{const r=await fetch(url,{credentials:'include',...opt});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||'Request failed');return d};
function toast(msg){const el=document.createElement('div');el.className='toast';el.textContent=msg;document.body.appendChild(el);setTimeout(()=>el.remove(),2800)}
function initials(name){return String(name||'?').split(/\s+/).slice(0,2).map(x=>x[0]).join('').toUpperCase()}
function nav(){const base=[['home','⌂'],['health','♥'],['meds','◉'],['appts','▣'],['family','♙'],['docs','▤'],['emergency','⚠'],['settings','⚙']];if(state.user?.role==='admin')base.push(['admin','▣']);return base}
function layout(){document.documentElement.dataset.theme=state.theme;document.body.style.fontSize=`${18*(1+[0,.15,.35][state.scale])}px`;const navHtml=nav().map(([k,ic])=>`<button class="${state.page===k?'active':''} ${k==='emergency'?'emergency':''}" data-page="${k}">${ic} &nbsp;${esc(t()[k])}</button>`).join('');return `<div class="shell"><header class="topbar"><div class="brand"><div class="logo">♥</div><div><h1>ElderCare</h1><p>${esc(t().tag)}</p></div></div><div class="top-actions"><select class="select" id="roleSelect" title="Viewing as"><option>${esc(state.user?.role||'')}</option></select><select class="select" id="lang"><option value="en">English</option><option value="hi">हिन्दी</option><option value="mr">मराठी</option></select><button class="btn secondary small" id="scale">A ${['Normal','Large','Largest'][state.scale]}</button><button class="btn secondary small" id="theme">☼ ${esc(t().theme)}</button><button class="btn outline small" id="logout">${esc(t().logout)}</button></div></header><div class="layout"><aside class="sidebar"><nav class="nav">${navHtml}</nav></aside><main class="main" id="main"></main></div><nav class="bottomnav">${navHtml}</nav><button class="fab" id="voiceFab" aria-label="Voice">⌕</button><div id="voiceMount"></div></div>`}
function render(){if(!state.user){renderLogin();return}document.getElementById('app').innerHTML=layout();document.getElementById('lang').value=state.lang;document.querySelectorAll('[data-page]').forEach(b=>b.onclick=()=>{state.page=b.dataset.page;render()});document.getElementById('lang').onchange=e=>{state.lang=e.target.value;localStorage.ec_lang=state.lang;render()};document.getElementById('scale').onclick=()=>{state.scale=(state.scale+1)%3;localStorage.ec_scale=state.scale;render()};document.getElementById('theme').onclick=()=>{state.theme=state.theme==='light'?'dark':state.theme==='dark'?'contrast':'light';localStorage.ec_theme=state.theme;render()};document.getElementById('logout').onclick=logout;document.getElementById('voiceFab').onclick=()=>voicePanel();document.getElementById('main').innerHTML=pages()[state.page]||pages().home;wire();}
function renderLogin(){document.getElementById('app').innerHTML=`<div class="login"><div class="login-card"><div class="brand"><div class="logo">♥</div><div><h1>ElderCare</h1><p>${esc(t().tag)}</p></div></div><h2>${esc(t().welcome)}</h2><p class="hint">${esc(t().login)} to continue.</p><div class="demo-list"><b>${esc(t().demo)}</b><div>sunita.demo@eldercare.example</div><div>rahul.demo@eldercare.example</div><div>dr.patil.demo@eldercare.example</div><div>caregiver.demo@eldercare.example</div><div>admin.demo@eldercare.example</div><code>${esc(t().passwordHint)}</code></div><form id="loginForm"><div class="field"><label>${esc(t().email)}</label><input name="email" type="email" value="sunita.demo@eldercare.example" required autocomplete="username"></div><div class="field"><label>${esc(t().password)}</label><input name="password" type="password" value="Demo!Password123" required autocomplete="current-password"></div><button class="btn primary block" type="submit">${esc(t().login)}</button></form><div class="disclaimer">${esc(t().disclaimer)}</div></div></div>`;document.getElementById('loginForm').onsubmit=async e=>{e.preventDefault();try{const f=new FormData(e.target);state.user=await api('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(f))}).then(x=>x.user);state.data=await api('/api/dashboard');state.page='home';render()}catch(err){toast(err.message)}}}
async function logout(){try{await api('/api/auth/logout',{method:'POST'})}catch{}state.user=null;state.data=null;render()}
function pages(){return {home:homePage(),health:healthPage(),meds:medPage(),appts:apptPage(),family:familyPage(),docs:docsPage(),emergency:emergencyPage(),settings:settingsPage(),admin:adminPage()}}
function head(title,sub,actions=''){return `<div class="page-head"><div><h2>${esc(title)}</h2><div class="muted">${esc(sub)}</div></div><div class="actions">${actions}</div></div>`}
function homePage(){const d=state.data,e=d.elder,next=d.nextAppointment,primary=d.primaryContact;const pending=d.medicines.flatMap(m=>m.schedule.map((s,i)=>({...s,m,i}))).filter(x=>x.status!=='taken').slice(0,4);return `${head(`${t().welcome}, ${e.name.split(' ')[0]}`,t().overview,`<button class="btn primary" data-go="emergency">⚠ ${esc(t().emergency)}</button>`)}<div class="grid cols3"><div class="card hero"><div class="muted tiny">${esc(t().blood)}</div><div class="stat">${esc(e.bloodGroup)}</div><div class="health-chips">${e.conditions.map(x=>`<span class="tag">${esc(x)}</span>`).join('')}</div></div><div class="card"><div class="muted tiny">${esc(t().next)}</div><div class="stat" style="font-size:1.1rem">${next?esc(next.date):'—'}</div><div>${next?esc(next.doctor):'No upcoming appointment'}</div></div><div class="card"><div class="muted tiny">${esc(t().family)}</div><div class="stat" style="font-size:1.1rem">${d.family.length}</div><div>${primary?esc(primary.name):'No primary contact'}</div></div></div><div class="grid cols2" style="margin-top:16px"><section class="card"><h3>${esc(t().meds)}</h3>${pending.length?pending.map(x=>`<div class="row"><div><b>${esc(x.m.name)}</b><div class="muted tiny">${esc(x.slot)} · ${esc(x.time)}</div></div><div class="actions"><span class="status ${x.status}">${esc(x.status)}</span><button class="btn primary small" data-med="${x.m.id}" data-slot="${x.i}" data-status="taken">${esc(t().mark)}</button></div></div>`).join(''):'<div class="empty">All scheduled doses are marked taken.</div>'}</section><section class="card"><h3>${esc(t().alerts||'Alerts')}</h3>${d.alerts.slice(0,4).map(a=>`<div class="row"><div><b>${esc(a.message)}</b><div class="muted tiny">${esc(a.time)}</div></div></div>`).join('')||'<div class="empty">No alerts</div>'}</section></div><div class="card emergency" style="margin-top:16px"><h3>Emergency quick access</h3><div class="call-grid"><button class="btn danger" data-call="${esc(primary?.phone||'')}" data-name="${esc(primary?.name||'Family contact')}">☎ ${esc(t().call)} family</button><button class="btn primary" data-call="+919021055678" data-name="${esc(e.primaryDoctor)}">☎ ${esc(t().call)} doctor</button><button class="btn primary" data-call="+912402345678" data-name="Hospital">☎ ${esc(t().call)} hospital</button></div></div><div class="disclaimer">${esc(t().disclaimer)}</div>`}
function healthPage(){const e=state.data.elder, can=['elder','doctor'].includes(state.user.role);return `${head(t().health,'Your health profile',can?`<button class="btn primary" id="editHealth">${esc(t().edit)}</button>`:'')}<div class="grid cols2"><section class="card"><h3>Profile</h3><div class="row"><span>${esc(t().blood)}</span><b>${esc(e.bloodGroup)}</b></div><div class="row"><span>${esc(t().conditions)}</span><div class="health-chips">${e.conditions.map(x=>`<span class="tag">${esc(x)}</span>`).join('')}</div></div><div class="row"><span>${esc(t().allergies)}</span><div class="health-chips">${e.allergies.map(x=>`<span class="tag">${esc(x)}</span>`).join('')}</div></div><div class="row"><span>Doctor</span><b>${esc(e.primaryDoctor)}</b></div><div class="row"><span>Hospital</span><b>${esc(e.hospital)}</b></div></section><section class="card"><h3>Care notes</h3><p>${esc(e.medicalNotes)}</p><div class="notice">Last updated: ${esc(e.lastUpdated)}</div><h3 style="margin-top:18px">Vaccinations</h3>${e.vaccinations.map(v=>`<div class="row"><span>${esc(v.name)}</span><span class="muted">${esc(v.date)}</span></div>`).join('')}</section></div><div class="disclaimer">${esc(t().disclaimer)}</div>`}
function medPage(){if(state.user.role==='admin')return restricted();const can=state.user.role!=='admin';return `${head(t().meds,'Daily medication schedule',can?`<button class="btn primary" id="addMed">+ Add medicine</button>`:'')}<div class="grid cols2">${state.data.medicines.map(m=>`<section class="card"><h3>${esc(m.name)} <span class="tag">${esc(m.dosage)}</span></h3><div class="muted tiny">${esc(m.purpose)} · ${esc(m.frequency)}</div><p class="tiny">${esc(m.instructions)}</p>${m.schedule.map((s,i)=>`<div class="row"><div><b>${esc(s.slot)}</b><div class="muted tiny">${esc(s.time)}</div></div><div class="actions"><span class="status ${s.status}">${esc(s.status)}</span><button class="btn primary small" data-med="${m.id}" data-slot="${i}" data-status="taken">${esc(t().taken)}</button><button class="btn secondary small" data-med="${m.id}" data-slot="${i}" data-status="pending">${esc(t().skip)}</button><button class="btn outline small" data-remind="${esc(m.name)}">${esc(t().remind)}</button></div></div>`).join('')}</section>`).join('')}</div>`}
function apptPage(){if(state.user.role==='admin')return restricted();const can=['elder','family','doctor'].includes(state.user.role);return `${head(t().appts,'Upcoming and past care visits',can?`<button class="btn primary" id="addAppt">+ ${esc(t().add)}</button>`:'')}<section class="card table-wrap"><table class="table"><thead><tr><th>Date</th><th>Doctor</th><th>Purpose</th><th>Location</th><th>Status</th><th></th></tr></thead><tbody>${state.data.appointments.map(a=>`<tr><td>${esc(a.date)}<br>${esc(a.time)}</td><td>${esc(a.doctor)}<br><span class="muted">${esc(a.hospital)}</span></td><td>${esc(a.purpose)}<br><span class="muted">${esc(a.notes)}</span></td><td>${esc(a.location)}</td><td><span class="status ${a.status==='upcoming'?'taken':a.status==='cancelled'?'missed':'pending'}">${esc(a.status)}</span></td><td>${can&&a.status==='upcoming'?`<button class="btn danger small" data-cancel-appt="${a.id}">${esc(t().cancel)}</button>`:''}</td></tr>`).join('')}</tbody></table></section>`}
function familyPage(){const can=state.user.role==='elder';return `${head(t().family,'People connected to care',can?'<button class="btn primary" id="addFamily">+ Add family</button>':'')}<div class="grid cols3">${state.data.family.map(f=>`<section class="card"><div class="person"><div class="avatar">${initials(f.name)}</div><div><b>${esc(f.name)}</b><div class="muted tiny">${esc(f.relation)} · ${esc(f.city)}</div></div></div><p class="tiny">${esc(f.phone)}<br>${esc(f.email)}</p><div class="actions"><button class="btn primary small" data-call="${esc(f.phone)}" data-name="${esc(f.name)}">☎ ${esc(t().call)}</button><a class="btn secondary small" href="mailto:${encodeURIComponent(f.email)}">✉ ${esc(t().message)}</a>${can&&!f.emergencyContact?`<button class="btn outline small" data-primary="${f.id}">Make primary</button>`:' '}${f.emergencyContact?'<span class="tag">Primary contact</span>':''}</div></section>`).join('')}</div>`}
function docsPage(){const canDelete=['elder','family'].includes(state.user.role);const can=state.user.role!=='admin'&&['elder','family','doctor'].includes(state.user.role);if(!can)return restricted();return `${head(t().docs,'Private document vault','<button class="btn primary" id="uploadBtn">＋ '+esc(t().upload)+'</button>')}<section class="card table-wrap"><table class="table"><thead><tr><th>Name</th><th>Type</th><th>Date</th><th>Uploaded by</th><th></th></tr></thead><tbody>${state.data.documents.map(d=>`<tr><td><b>${esc(d.name)}</b></td><td>${esc(d.type)}</td><td>${esc(d.date)}</td><td>${esc(d.uploadedBy)}</td><td class="actions">${d.filename?`<button class="btn outline small" data-download="${d.id}">Download</button>`:''}${canDelete?`<button class="btn danger small" data-delete-doc="${d.id}">${esc(t().delete)}</button>`:''}</td></tr>`).join('')}</tbody></table></section>`}
function emergencyPage(){if(state.user.role==='admin')return restricted();const e=state.data.elder,p=state.data.primaryContact;return `${head(t().emergency,'Keep urgent contacts easy to reach')}<section class="card emergency"><h3>Emergency contacts</h3><div class="call-grid"><button class="btn danger" data-call="112" data-name="Emergency services">☎ 112</button><button class="btn primary" data-call="${esc(p?.phone||'')}" data-name="${esc(p?.name||'Primary family contact')}">☎ Family</button><button class="btn primary" data-call="+919021055678" data-name="${esc(e.primaryDoctor)}">☎ Doctor</button></div><p class="muted tiny" style="margin-top:14px">For a life-threatening emergency, contact your local emergency service immediately.</p></section><div class="grid cols2" style="margin-top:16px"><section class="card"><h3>Important health information</h3><div class="row"><span>${esc(t().blood)}</span><b>${esc(e.bloodGroup)}</b></div><div class="row"><span>${esc(t().allergies)}</span><b>${esc(e.allergies.join(', '))}</b></div></section><section class="card"><h3>Current medicines</h3>${state.data.medicines.map(m=>`<div class="row"><b>${esc(m.name)}</b><span>${esc(m.dosage)}</span></div>`).join('')}</section></div>`}
function settingsPage(){return `${head(t().settings,'Accessibility and preferences')}<div class="grid cols2"><section class="card"><h3>${esc(t().language)}</h3><div class="actions"><button class="btn ${state.lang==='en'?'primary':'secondary'}" data-lang="en">English</button><button class="btn ${state.lang==='hi'?'primary':'secondary'}" data-lang="hi">हिन्दी</button><button class="btn ${state.lang==='mr'?'primary':'secondary'}" data-lang="mr">मराठी</button></div></section><section class="card"><h3>${esc(t().theme)}</h3><div class="actions"><button class="btn ${state.theme==='light'?'primary':'secondary'}" data-theme="light">Light</button><button class="btn ${state.theme==='dark'?'primary':'secondary'}" data-theme="dark">Dark</button><button class="btn ${state.theme==='contrast'?'primary':'secondary'}" data-theme="contrast">High contrast</button></div></section><section class="card"><h3>${esc(t().text)}</h3><div class="actions"><button class="btn secondary" data-scale="0">Normal</button><button class="btn secondary" data-scale="1">Large</button><button class="btn secondary" data-scale="2">Largest</button></div></section><section class="card"><h3>Session</h3><button class="btn outline" id="logout2">${esc(t().logout)}</button><p class="muted tiny">Role: ${esc(state.user.role)} · Signed-in session is stored server-side.</p></section></div>`}
function adminPage(){if(state.user.role!=='admin')return restricted();return `${head(t().admin,'Account and audit controls')}<div class="notice">${esc(t().adminNote)}</div><div class="grid cols2" style="margin-top:16px"><section class="card"><h3>Users</h3><div class="list">${state.data.adminUsers?.map(u=>`<div class="row"><div><b>${esc(u.name)}</b><div class="muted tiny">${esc(u.email)}</div></div><span class="tag">${esc(u.role)}</span></div>`).join('')||'<div class="empty">Loading…</div>'}</div></section><section class="card"><h3>Audit log</h3>${state.data.adminAudit?.slice(0,15).map(x=>`<div class="row"><div><b>${esc(x.action)}</b><div class="muted tiny">${esc(x.who)} · ${esc(x.time)}</div></div></div>`).join('')||'<div class="empty">No audit records</div>'}</section></div>`}
function restricted(){return `<section class="card empty"><div style="font-size:2rem">🔒</div><h3>${esc(t().restricted)}</h3><p class="muted">Your account does not have permission to view this data.</p></section>`}
function wire(){document.querySelectorAll('[data-go]').forEach(b=>b.onclick=()=>{state.page=b.dataset.go;render()});document.querySelectorAll('[data-med]').forEach(b=>b.onclick=()=>markMed(b.dataset.med,Number(b.dataset.slot),b.dataset.status));document.querySelectorAll('[data-remind]').forEach(b=>b.onclick=()=>toast(`Reminder set for ${b.dataset.remind}`));document.querySelectorAll('[data-call]').forEach(b=>b.onclick=()=>callConfirm(b.dataset.name,b.dataset.call));document.querySelectorAll('[data-cancel-appt]').forEach(b=>b.onclick=()=>cancelAppt(b.dataset.cancelAppt));document.querySelectorAll('[data-primary]').forEach(b=>b.onclick=()=>primaryFamily(b.dataset.primary));document.querySelectorAll('[data-delete-doc]').forEach(b=>b.onclick=()=>deleteDoc(b.dataset.deleteDoc));document.querySelectorAll('[data-download]').forEach(b=>b.onclick=()=>location.href='/api/documents/'+b.dataset.download+'/download');document.querySelectorAll('[data-lang]').forEach(b=>b.onclick=()=>{state.lang=b.dataset.lang;localStorage.ec_lang=state.lang;render()});document.querySelectorAll('[data-theme]').forEach(b=>b.onclick=()=>{state.theme=b.dataset.theme;localStorage.ec_theme=state.theme;render()});document.querySelectorAll('[data-scale]').forEach(b=>b.onclick=()=>{state.scale=Number(b.dataset.scale);localStorage.ec_scale=state.scale;render()});document.getElementById('logout2')?.addEventListener('click',logout);document.getElementById('editHealth')?.addEventListener('click',editHealth);document.getElementById('addAppt')?.addEventListener('click',addAppt);document.getElementById('addMed')?.addEventListener('click',addMed);document.getElementById('addFamily')?.addEventListener('click',addFamily);document.getElementById('uploadBtn')?.addEventListener('click',uploadDoc);if(state.page==='admin')loadAdmin();}
async function markMed(id,slot,status){try{await api(`/api/medications/${id}/mark`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({slotIndex:slot,status})});state.data=await api('/api/dashboard');render();toast(status==='taken'?'Medicine marked taken':'Medicine updated')}catch(e){toast(e.message)}}
function callConfirm(name,num){if(!num)return toast('No phone number available');if(confirm(`${t().confirm} Call ${name} (${num})?`)){toast(`Calling ${name}`);location.href=`tel:${String(num).replace(/\s/g,'')}`}}
async function cancelAppt(id){if(!confirm(t().confirm))return;try{await api('/api/appointments/'+id,{method:'DELETE'});state.data=await api('/api/dashboard');render();toast('Appointment cancelled')}catch(e){toast(e.message)}}
async function primaryFamily(id){try{await api('/api/family-members/'+id,{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({emergencyContact:true})});state.data=await api('/api/dashboard');render();toast('Primary contact updated')}catch(e){toast(e.message)}}
async function deleteDoc(id){if(!confirm(t().confirm))return;try{await api('/api/documents/'+id,{method:'DELETE'});state.data=await api('/api/dashboard');render();toast('Document deleted')}catch(e){toast(e.message)}}
function modal(title,body){const m=document.createElement('div');m.className='modal-backdrop';m.innerHTML=`<div class="modal"><div class="modal-head"><h3>${esc(title)}</h3><button class="btn secondary small" data-close>✕</button></div>${body}</div>`;m.querySelector('[data-close]').onclick=()=>m.remove();document.body.appendChild(m);return m}
function editHealth(){const e=state.data.elder;const m=modal(t().health,`<form id="healthForm"><div class="field"><label>Blood group</label><input name="bloodGroup" value="${esc(e.bloodGroup)}"></div><div class="field"><label>Conditions (comma separated)</label><input name="conditions" value="${esc(e.conditions.join(', '))}"></div><div class="field"><label>Allergies (comma separated)</label><input name="allergies" value="${esc(e.allergies.join(', '))}"></div><div class="field"><label>Care notes</label><textarea name="medicalNotes">${esc(e.medicalNotes)}</textarea></div><div class="actions"><button class="btn primary">${esc(t().save)}</button></div></form>`);m.querySelector('form').onsubmit=async e2=>{e2.preventDefault();const f=new FormData(e2.target),o=Object.fromEntries(f);o.conditions=o.conditions.split(',').map(x=>x.trim()).filter(Boolean);o.allergies=o.allergies.split(',').map(x=>x.trim()).filter(Boolean);try{await api('/api/medical-profile',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify(o)});m.remove();state.data=await api('/api/dashboard');render();toast('Health profile saved')}catch(e){toast(e.message)}}}
function addAppt(){const m=modal(t().add,`<form id="apptForm"><div class="field"><label>Doctor</label><input name="doctor" required></div><div class="field"><label>Hospital</label><input name="hospital"></div><div class="field"><label>Date</label><input name="date" type="date" required></div><div class="field"><label>Time</label><input name="time" type="time"></div><div class="field"><label>Purpose</label><input name="purpose"></div><div class="field"><label>Location</label><input name="location"></div><div class="actions"><button class="btn primary">${esc(t().save)}</button></div></form>`);m.querySelector('form').onsubmit=async e=>{e.preventDefault();try{await api('/api/appointments',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(e.target)))});m.remove();state.data=await api('/api/dashboard');render();toast('Appointment added')}catch(e){toast(e.message)}}}
function addMed(){const m=modal('Add medicine',`<form id="medForm"><div class="field"><label>Name</label><input name="name" required></div><div class="field"><label>Purpose</label><input name="purpose"></div><div class="field"><label>Dosage</label><input name="dosage"></div><div class="field"><label>Frequency</label><input name="frequency" value="Once daily"></div><div class="field"><label>Instructions</label><textarea name="instructions"></textarea></div><div class="actions"><button class="btn primary">${esc(t().save)}</button></div></form>`);m.querySelector('form').onsubmit=async e=>{e.preventDefault();try{await api('/api/medications',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(e.target)))});m.remove();state.data=await api('/api/dashboard');render();toast('Medicine added')}catch(e){toast(e.message)}}}
function addFamily(){const m=modal('Add family member',`<form id="famForm"><div class="field"><label>Name</label><input name="name" required></div><div class="field"><label>Relation</label><input name="relation"></div><div class="field"><label>Phone</label><input name="phone"></div><div class="field"><label>Email</label><input name="email" type="email"></div><div class="field"><label>City</label><input name="city"></div><button class="btn primary">${esc(t().save)}</button></form>`);m.querySelector('form').onsubmit=async e=>{e.preventDefault();try{await api('/api/family-members',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(e.target)))});m.remove();state.data=await api('/api/dashboard');render();toast('Family member added')}catch(e){toast(e.message)}}}
function uploadDoc(){const m=modal(t().upload,`<form id="docForm" enctype="multipart/form-data"><div class="field"><label>File</label><input name="file" type="file" required></div><div class="field"><label>Type</label><select name="type"><option>Prescription</option><option>Blood report</option><option>Scan report</option><option>Insurance document</option><option>Other</option></select></div><div class="field"><label>Display name</label><input name="name" placeholder="Optional"></div><button class="btn primary">${esc(t().upload)}</button></form>`);m.querySelector('form').onsubmit=async e=>{e.preventDefault();try{await api('/api/documents',{method:'POST',body:new FormData(e.target)});m.remove();state.data=await api('/api/dashboard');render();toast('Document uploaded')}catch(e){toast(e.message)}}}
async function loadAdmin(){try{state.data.adminUsers=await api('/api/admin/users');state.data.adminAudit=await api('/api/admin/audit-log');document.getElementById('main').innerHTML=adminPage();}catch(e){toast(e.message)}}
function voicePanel(){const m=document.createElement('div');m.className='voice-panel';m.innerHTML=`<div class="modal-head"><h3>${esc(t().voice)}</h3><button class="btn secondary small" data-close>✕</button></div><div class="voice-log">${state.voice.length?state.voice.map(v=>`<div class="voice-item"><b>“${esc(v.q)}”</b><div class="muted">${esc(v.a)}</div></div>`).join(''):'<div class="empty">Try “show my medicines”, “next appointment”, or “call my son”.</div>'}</div><form id="voiceForm" class="actions"><input class="select" style="flex:1" name="q" placeholder="Type a command…" required><button class="btn primary small">Go</button></form>`;m.querySelector('[data-close]').onclick=()=>m.remove();m.querySelector('form').onsubmit=e=>{e.preventDefault();const q=new FormData(e.target).get('q').toLowerCase();let a='I can help with medicines, appointments, and emergency contacts.';if(q.includes('medicine')){state.page='meds';a='Opening your medicine schedule.'}else if(q.includes('appointment')){state.page='appts';a='Opening your appointments.'}else if(q.includes('emergency')){state.page='emergency';a='Opening emergency contacts.'}else if(q.includes('family')||q.includes('son')){state.page='family';a='Opening your family contacts.'}state.voice.unshift({q,a});m.remove();render();setTimeout(voicePanel,0)};document.body.appendChild(m)}
(async()=>{try{const s=await api('/api/auth/session');state.user=s.user;state.data=await api('/api/dashboard')}catch{}render()})();
